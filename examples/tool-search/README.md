# Tool Search Agent

ADK agent with adk-tool-search for dynamic BM25-based tool discovery.

## Usage

```bash
amh optimize \
  --dataset path/to/tasks \
  --initial-harness examples/tool-search/initial_harness \
  --proposer opencode
```