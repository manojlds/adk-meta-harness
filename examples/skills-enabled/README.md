# Skills-Enabled Agent

ADK agent with adk-skills discovery and activation.

## Usage

```bash
amh optimize \
  --dataset path/to/tasks \
  --initial-harness examples/skills-enabled/initial_harness \
  --proposer opencode
```