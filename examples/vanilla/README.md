# Vanilla Agent

Minimal ADK agent with no skills, tools, or callbacks.

## Usage

```bash
amh optimize \
  --dataset path/to/tasks \
  --initial-harness examples/vanilla/initial_harness \
  --proposer opencode
```